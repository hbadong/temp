- 通用工具函数集中挂载到全局命名空间（如`le.xxx`、`window.adminAjax.xxx`），避免污染全局作用域的同时保持调用简洁。
- 第三方库以压缩版（`.min.js`）形式存放，便于生产环境直接引用。
- Layui扩展模块遵循`lay-module/<模块名>/<模块名>.js`的目录结构，并通过`lay-config.js`的`extend`统一注册。
- 后台AJAX接口统一返回JSON格式 `{err, msg}`，错误时icon=5，成功时自动刷新页面。