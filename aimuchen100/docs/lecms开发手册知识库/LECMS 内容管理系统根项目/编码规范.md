- 所有 PHP 文件顶部统一使用 Author/Date/Time/Description 注释块
- 路径常量通过 define() 在入口集中声明（ROOT_PATH、APP_PATH、FRAMEWORK_PATH），子模块通过相对路径引用
- 业务功能以独立目录形式挂载于根目录（如 tools_module、lottery_templates、admin、install），通过统一入口路由分发
- 模板文件遵循 view/default/ 目录结构，静态资源集中于 static/ 目录并按模块子目录组织