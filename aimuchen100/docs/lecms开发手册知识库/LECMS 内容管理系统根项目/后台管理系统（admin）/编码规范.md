- 每个业务模块对应一个 *_control.class.php 控制器文件，方法名与 view/*.htm 模板一一对应
- 控制器统一继承 admin_control 基类，复用登录校验、权限检查与公共渲染逻辑
- 模板文件集中存放在 view/default/ 目录，命名采用 模块_动作.htm 或 inc-前缀包含公共片段
- 列表页统一使用 layui table 组件展示数据，批量操作通过复选框+表单提交实现